<header class="navbar navbar-fixed-top navbar-inverse">
  <div class="container">
    <nav>
      <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
          <li><%= t('header.welcome') %>, <b><%= current_user.first_name %></b> <%= t('header.guru') %></li>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown"> <%= t('header.dropdown') %><b class="caret"></b> </a>
            <ul class="dropdown-menu">
              <li>
                <%= link_to t('header.logout'), destroy_user_session_path, method: :delete %>
              </li>
            </ul>
          </li>
        <% else %>
          <li><%= link_to t('common.login'), new_user_session_path %></li>
        <% end %>
      </ul>
    </nav>
  </div>
</header>
